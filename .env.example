## Example environment variables
# Do NOT commit real keys. Keep actual keys in your environment or CI settings.

# Example server-only provider key (used by server-side /api/intelligence calls)
DEEPSEEK_API_KEY=

# Local development: set NODE_ENV=development if needed
NODE_ENV=development
