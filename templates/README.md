# Itinerary Templates

Place `美国行程260701.xlsx` here when the original workbook is available. The MVP upload flow can also ingest an Excel file from the browser and will fall back to the built-in mock trip if parsing is incomplete.
